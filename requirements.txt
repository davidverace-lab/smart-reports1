# ============================================
# SMART REPORTS - DEPENDENCIES
# Instituto Hutchison Ports v2.0
# ============================================

# Base de datos
pyodbc>=5.0.0                      # SQL Server
mysql-connector-python>=8.0.33     # MySQL

# Procesamiento de datos
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0                    # Excel

# Interfaz gráfica
customtkinter>=5.2.0               # UI moderna
tkinterdnd2-universal>=1.7.3       # Drag & drop
pillow>=10.0.0                     # Imágenes

# Gráficos y visualización
pywebview>=4.3.3                   # Embeber HTML/JS/D3.js en ventanas nativas (PRINCIPAL - Usado activamente)
# Librerías legacy (ya no se usan activamente, reemplazadas por D3.js):
# plotly>=5.17.0                   # Legacy: Reemplazado por D3.js
# matplotlib>=3.7.0                # Legacy: Reemplazado por D3.js
# seaborn>=0.12.0                  # Legacy: Reemplazado por D3.js

# Reportes PDF
reportlab>=4.0.0                   # Generación de PDFs

# Utilidades
python-dateutil>=2.8.2             # Manejo de fechas
colorama>=0.4.6                    # Colores en terminal (debugging)

# Empaquetado
pyinstaller>=6.0.0

# NOTAS DE INSTALACIÓN:
# =====================
# 1. Ejecutar: pip install -r requirements.txt
# 2. pywebview requiere navegador del sistema (Edge/Chrome en Windows)
# 3. Para desarrollo: pip install --upgrade --force-reinstall customtkinter
# 4. PIL viene incluido con pillow, no instalar por separado
# 5. D3.js se carga desde CDN (sin instalación local necesaria)
